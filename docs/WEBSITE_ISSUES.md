# KVJ Analytics — Website Issues Log

> Running list of issues to fix, collected from the CEO. Do NOT start fixing until
> explicitly told. Each issue gets triaged and fixed one by one, logically.
> Status: `open` → `in-progress` → `fixed`.

| # | Area / Page | Issue (as reported) | Notes / Root cause | Status |
|---|-------------|---------------------|--------------------|--------|
| 1 | Home → "Trusted By" logos (sourced from Clients module) | Source Home logo strip from the admin "Clients" module. | ALREADY DONE in code — home wall reads the `clients` table (logo_url → falls back to name); admin Clients form uses the shared `ImageField` (share-link convert + preview). Broken "MIM" logo = stale data for that client → re-save it. Needs deploy. | ✅ done in code |
| 2 | Home → "Industries" section (`IndustryGrid.tsx`) | Grid alignment is uneven (5 cards top row, 3 bottom). Want it to auto-balance intelligently (e.g. 4×4) for any item count. | DONE — added `bestCols()` that picks 3/4/5 columns to minimise orphan cards for any item count (8 → 4×4). | ✅ fixed |
| 3 | Home → "Our Approach" pipeline (`BusinessAnalyticsPipeline`) + Admin CMS | Admin editor didn't match the live site (admin: Discover/Design/Build/Deploy; live: 7 stages). | DONE — replaced phantom `approach.steps` with the real 7 stages; pipeline now reads stage labels from CMS by position (`stageNames`). Editable via the Home editor's "Our Approach" steps; admin now matches the site. | ✅ fixed |
| 4 | About page → intro "16+ years of experience" | "16+" should auto-increment yearly (17+ next year, etc.), not be hardcoded. | DONE — exported `FOUNDING_YEAR=2010` + `CURRENT_EXP_YEARS` in constants; routed remaining hardcoded "16" strings (About intro, Impact intro, training bullet, layout SEO) through it. **Confirm 2010 is the correct founding year** — if not, change the one constant. (mockSupabase demo bios still say 16 — demo data only.) | ✅ fixed (confirm year) |
| 5 | About page → bottom CTA ("Let's Build Smarter Systems Together") | No option in admin to modify this CTA. | DONE — added `cta` to `FALLBACK_ABOUT`, reusable `CtaEditor` ("Bottom Call-to-Action" card) in admin, About page renders `page.cta`. | ✅ fixed |
| 6 | Corporate Solutions page → bottom CTA ("Looking for custom automation, reports or dashboards?") | No option in admin to edit it. | DONE — `cta` in `FALLBACK_CORPORATE`, CtaEditor in Corporate admin, page renders `page.cta`. | ✅ fixed |
| 7 | Educational Solutions page → bottom CTA ("Looking to run a certificate program or skill lab?") | No option in admin to edit it. | DONE — `cta` in `FALLBACK_EDUCATION`, CtaEditor in Education admin, page renders `page.cta`. | ✅ fixed |
| 8 | Product / Educational detail-page side "request" cards | Side-card (heading, description, bullets, button) not editable in admin. | DONE — added `demoCard` (Products) + `partnerCard` (Education) to CMS, render from it on the detail pages, and added a reusable "side card" editor (heading, description, bullet list, button label) in the Products & Education admin editors. (Also removed the fabricated "50,000+" bullet.) | ✅ fixed |
| 9 | Cards with animated visuals (e.g. chip/"PROTRIX" animation) — admin | **DONE for product cards:** the built-in preset library already existed (`ProductHolograms` / `HOLOGRAM_MAP`, 13 animations incl. the PROTRIX chip). Added (a) an admin picker to choose a preset per product, and (b) a "Custom (paste HTML)" option rendered in a **sandboxed iframe** (`SandboxedAnimation.tsx`, `sandbox="allow-scripts"`, isolated origin). Remaining follow-up: reuse the same slot on other card types (Industries/Solutions/Training) — mechanical. | When adding a new card, need an option to attach an animation like the sample. Two ideas from CEO: (a) paste HTML animation generated via Gemini, or (b) pre-store ~15 animations in admin and select one per card. | **Recommended architecture (decide at build):** hybrid. (1) Curated preset library stored in a DB table `animation_presets` (Lottie JSON / lightweight SVG-CSS), pick-from-gallery per card with live preview, extendable via admin without deploy; (2) advanced "paste custom HTML/SVG" rendered in a **sandboxed iframe** (`sandbox="allow-scripts"`, no same-origin) to prevent XSS/layout breakage. Per-card control: None / Library / Custom. **CEO answer: animation slot for ALL cards site-wide** (Products, Industries, Solutions, Training, everywhere) → makes the DB-backed reusable preset system essential. | open |
| 10 | Training page → Hero section (`TrainingHubClient.tsx`) | Redesign the Training hero (CEO: no dashboard/mock UI — must look professional). | DONE — editorial, text-forward hero: calm eyebrow, oversized headline, subtext, dual CTA (Explore Courses + Student Portal Login), and a "Core disciplines" strip (Advanced Excel, Power BI, Data Analytics, Dashboards, Report Automation, Business Intelligence). Removed the mock "learning console" dashboard entirely. Copy stays CMS-driven. | ✅ fixed |
| 11 | Training page → "The Learning Journey" / Curriculum Flow section | No proper option to edit these stages in admin. | DONE — added `journey` to `FALLBACK_TRAINING_HUB`, section renders from CMS (`hub.journey`), and the Training Hub admin editor now has a "The Learning Journey" panel (add/remove/edit stage step, name, description, icon). | ✅ fixed |
| 12 | Training page → "Integrated Learning Tools" / Ecosystem section | Not editable in admin. | DONE — added `tools` to `FALLBACK_TRAINING_HUB`, section renders from CMS (`hub.tools`), and the admin editor has an "Integrated Learning Tools" panel (add/remove/edit label, description, icon). | ✅ fixed |
| 13 | Training page → "Student Portal / Designed for Interactive Excellence" section | REMOVE this whole section (stats 50,000+ / 15+ / 20+ / 98% + desktop & mobile portal mockup). | ALREADY REMOVED in current code (git commit 80e625b "remove Student Dashboard Showcase section"). It only still shows on the LIVE site because the live site is an older deploy. → Resolves on next deploy. | ✅ done in code (needs deploy) |
| 14 | Blog page → posts vs Admin | Blog page shows posts not editable in admin. | DONE — public blog already reads DB-first (demo posts only show when the table is empty). Added `supabase/migrations/seed_blog_posts.sql` to insert the 3 starter posts as real, editable `blog_posts` rows (idempotent). **Action: run that SQL once in Supabase.** | ✅ fixed (run SQL) |
| 15 | Global → Admin nav/page visibility | Turn each tab/page ON/OFF. | DONE — added `pageVisibility` toggles in Global Settings admin; Header hides OFF pages from the nav; new `src/middleware.ts` redirects a disabled page's direct URL to home (60s-cached visibility lookup). Home can't be disabled. | ✅ fixed |
| 16 | Testimonials & Case Studies → not displayed on site | Neither shown on the public site. | DONE — new `CaseStudiesSection` + `TestimonialsSection` components render live admin data on the Impact page; both self-hide when there's no active data. | ✅ fixed |

## Known from prior analysis (not yet in the numbered list)
- Inner-page scroll animations fire at wrong position — Lenis never wired to GSAP ScrollTrigger in `SmoothScroll.tsx`.
- `ScrollFx.tsx` has TypeScript errors (unsound ref cast on SVG-capable `as` prop).
- Two parallel animation systems (CSS `Reveal` on home vs GSAP `ScrollFx` elsewhere) → inconsistent motion.
